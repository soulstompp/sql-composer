# Technic sets — standalone query, also used as a :union() source.
SELECT s.set_num, s.name, s.year, s.num_parts, 'Technic' AS theme_group
FROM lego_sets s
JOIN lego_themes t ON t.id = s.theme_id
WHERE t.name = :bind(technic_theme)
  AND s.year >= :bind(min_year)
